# Bootstrap pin for uv itself - NOT a dependency list.
#
# uv installs everything else from uv.lock, so it is the one tool that has to be
# obtained before a lock file can be honoured. It is pinned by version and wheel
# hash (linux x86_64 and aarch64), and installed with pip --require-hashes.
#
# To bump:
#   curl -s https://pypi.org/pypi/uv/json | \
#     jq -r '.info.version, (.urls[] | select(.filename | test("manylinux.*(x86_64|aarch64)") and (test("musl") | not)) | "\(.filename) \(.digests.sha256)")'
uv==0.12.1 \
    --hash=sha256:27211df9b277f440dea438a4e525ba40250fb721ad39b8927eefc2d91f9aea15 \
    --hash=sha256:b255ac23958e45f39f9c7a4cd65890df5ef46f539a3b14de03bd296bbba9cb60
